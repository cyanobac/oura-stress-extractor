# Python
__pycache__/
*.pyc
.venv/
venv/
debug_ocr_*.png

# Local SQLite DBs (dev defaults when RATE_LIMIT_DB / REQUEST_LOG_DB are unset)
ratelimit.db
ratelimit.db-wal
ratelimit.db-shm
requests.db
requests.db-wal
requests.db-shm

# Node / Vite
node_modules/
dist/
*.tsbuildinfo

# Env / secrets
.env
# Cloudflare Origin cert/key, mounted into Caddy in production — never commit.
certs/

# OS
.DS_Store
Thumbs.db
